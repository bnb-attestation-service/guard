---
description: How to use AgentGuard in plain language — what you can ask, nothing to memorise.
---

Print the usage card from the `agentguard-audit` skill's `references/usage.md`, exactly as
written.

Then, only if `command -v aguard` finds nothing, add one line: AgentGuard is not installed on
this machine yet — run `/aguard-setup`.

Nothing else: no scan, no config change.
